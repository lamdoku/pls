'use client';
import {useState} from 'react';import {supabase} from '@/lib/supabase';import {useRouter} from 'next/navigation';
export default function Signup(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [role,setRole]=useState('student');const [err,setErr]=useState('');const [ok,setOk]=useState(false);const r=useRouter();async function submit(e:any){e.preventDefault();setErr('');const {error}=await supabase().auth.signUp({email,password,options:{data:{role}}});if(error){setErr(error.message);return}setOk(true);if(role!=='teacher')r.push('/'+role)}return <main className="min-h-screen grid place-items-center p-6 bg-slate-50"><form onSubmit={submit} className="card p-8 w-full max-w-md"><div className="text-blue-600 font-bold text-sm">PERSONAL LEARNING SYSTEM</div><h1 className="text-3xl font-black mt-2">Create account</h1><p className="muted mt-2">Teacher accounts require Admin approval before professional access.</p><input required type="email" className="input mt-6" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input required minLength={8} type="password" className="input mt-3" placeholder="Password (8+ characters)" value={password} onChange={e=>setPassword(e.target.value)}/><select className="input mt-3" value={role} onChange={e=>setRole(e.target.value)}><option value="student">Student</option><option value="parent">Parent</option><option value="teacher">Teacher</option></select>{err&&<p className="text-red-600 text-sm mt-3">{err}</p>}{ok&&<p className="text-green-700 text-sm mt-3">Account created. Check email confirmation if enabled.</p>}<button className="btn btn-primary w-full mt-5">Create account</button></form></main>}