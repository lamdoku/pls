'use client';import {useState} from 'react';import Shell from '@/components/Shell';import {supabase} from '@/lib/supabase';export default function ParentLink(){const [code,setCode]=useState('');const [msg,setMsg]=useState('');async function claim(){const {error}=await supabase().rpc('claim_student_linking_code',{p_code:code});setMsg(error?error.message:'Learner linked successfully.');if(!error)setCode('')}return <Shell role="parent"><div className="max-w-xl"><h1 className="text-4xl font-black">Link your learner</h1><p className="muted mt-2">Enter the linking code generated by your child.</p><div className="card p-7 mt-6"><input className="input" placeholder="12-character linking code" value={code} onChange={e=>setCode(e.target.value.toUpperCase())}/><button disabled={!code} className="btn btn-primary mt-4" onClick={claim}>Connect</button>{msg&&<p className="mt-4">{msg}</p>}</div></div></Shell>}