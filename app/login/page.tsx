'use client';import {useState} from 'react';import Link from 'next/link';import {supabase} from '@/lib/supabase';import {useRouter} from 'next/navigation';export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [err,setErr]=useState('');const r=useRouter();async function go(e:any){e.preventDefault();setErr('');const {error}=await supabase().auth.signInWithPassword({email,password});if(error){setErr(error.message);return}const {data}=await supabase().from('users').select('role,status').single();if(data?.status==='blocked'){await supabase().auth.signOut();setErr('This account is blocked.');return}if(data?.role==='teacher'&&data.status==='pending'){setErr('Teacher account is awaiting Admin approval.');return}r.push('/'+(data?.role||'student'))}return <main className="min-h-screen grid place-items-center p-6 bg-slate-50"><form onSubmit={go} className="card p-8 w-full max-w-md"><div className="text-blue-600 font-bold text-sm">PERSONAL LEARNING SYSTEM</div><h1 className="text-3xl font-black mt-2">Welcome back</h1><input required className="input mt-6" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input required className="input mt-3" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/>{err&&<p className="text-red-600 text-sm mt-3">{err}</p>}<button className="btn btn-primary w-full mt-5">Sign in</button><Link className="block text-center text-blue-600 mt-4 text-sm" href="/signup">Create an account</Link></form></main>}